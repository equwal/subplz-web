-r requirements.txt
pytest>=8
httpx>=0.27

# Property tests (tests/test_burst_controller.py, tests/test_copy_database.py).
hypothesis>=6.100
# The burst controller reads the rq queue. Its tests use a fake Redis.
rq>=2.0
redis>=5.0
fakeredis>=2.20
