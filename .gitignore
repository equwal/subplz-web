# Runtime state: uploads, artifacts, the job database.
data/

# Environments
.venv/
venv*/

# Local config — copy .env.example and edit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# Whole-book test input; local only
tests/engine/golden-local/

# Browser-side libraries; tools/fetch_vendor.py downloads them, pinned
frontend/vendor/
tests/engine/excerpt-local/

# Test tooling for the browser engine
node_modules/

# Terraform (infra/burst): providers, and state files, which hold passwords.
# The state lives in /var/lib/subplz-burst on the web server, not here.
.terraform/
*.tfstate
*.tfstate.*
crash.log
